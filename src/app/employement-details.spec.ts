import { EmployementDetails } from './employement-details';

describe('EmployementDetails', () => {
  it('should create an instance', () => {
    expect(new EmployementDetails()).toBeTruthy();
  });
});
